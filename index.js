const {
  _CommandHandler,
  Command,
  Replacer,
  Await
} = require('./src/modules')
const Agent = require('./src/agent')

module.exports = {
  _CommandHandler,
  Agent,
  Command,
  Replacer,
  Await
}
